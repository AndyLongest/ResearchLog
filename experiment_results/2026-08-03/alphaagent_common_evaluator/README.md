# Native-gate and common-evaluator panels (v4)

This non-overwriting update fills the AlphaAgent common-evaluator row from all 16 eligible frozen v5 predictions. The four original failed or timeout proposal trials were not replaced. AlphaForge, AlphaPROBE, and RD-Agent(Q) remain visible and non-computable rather than being replaced by proxies or mismatched native metrics.
